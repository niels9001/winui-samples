<!--
//*********************************************************
//
// Copyright (c) Microsoft. All rights reserved.
// THIS CODE IS PROVIDED *AS IS* WITHOUT WARRANTY OF
// ANY KIND, EITHER EXPRESS OR IMPLIED, INCLUDING ANY
// IMPLIED WARRANTIES OF FITNESS FOR A PARTICULAR
// PURPOSE, MERCHANTABILITY, OR NON-INFRINGEMENT.
//
//*********************************************************
-->
<Page
    x:Class="SDKTemplate.Scenario13"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:SDKTemplate"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid x:Name="LayoutRoot">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid x:Name="Input" Grid.Row="0">
            <StackPanel >
                <TextBlock Margin="0,0,0,10" TextWrapping="Wrap" Style="{StaticResource BodyTextBlockStyle}" >
                    Files deployed with your app can be accessed via code. File variants marked for language, DX feature level, or other qualifiers can be resolved through the MRT Core Files resource map.
                </TextBlock>
                <TextBlock Margin="0,0,0,10" TextWrapping="Wrap" Style="{StaticResource BodyTextBlockStyle}" >
                    This scenario demonstrates access via code of a file resource, appdata/appdata.dat. There are three variants of this
                    resource, using the DXFeatureLevel qualifier. The resource reference used is Files/appdata/appdata.dat.
                </TextBlock>
                <TextBlock Margin="0,0,0,10" TextWrapping="Wrap" Style="{StaticResource BodyTextBlockStyle}" >
                    Resource context values for some qualifiers, such as language or scale, are set automatically by the system. For
                    certain qualifiers, however, the app is expected to set context qualifier values as appropriate. The
                    DXFeatureLevel qualifier is like that. This port creates a ResourceContext, sets the DXFeatureLevel qualifier value, resolves the best file candidate from the MRT Core Files subtree, and reads the resolved file path.
                </TextBlock>
                <TextBlock Margin="0,20,0,10" Text="Select a DXFeatureLevel value to set for the app:" Style="{StaticResource BodyTextBlockStyle}" />
                <ComboBox x:Name="DXFLOptionCombo" Width="120" SelectedValuePath="Content" SelectionChanged="DXFLOptionCombo_SelectionChanged" HorizontalAlignment="Left">
                    <ComboBoxItem>DX9</ComboBoxItem>
                    <ComboBoxItem>DX10</ComboBoxItem>
                    <ComboBoxItem>DX11</ComboBoxItem>
                </ComboBox>
            </StackPanel>
        </Grid>

        <Grid x:Name="Output" HorizontalAlignment="Left" VerticalAlignment="Top" Grid.Row="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <StackPanel Grid.Row="0">
                <TextBlock Margin="0,0,0,10" Text="Data read from file:" Style="{StaticResource BodyTextBlockStyle}" />
                <TextBlock x:Name="ResultText"  Style="{StaticResource BodyTextBlockStyle}" />
            </StackPanel>
        </Grid>

        <!-- Add Storyboards to the visual states below as necessary for supporting the various layouts -->
        <VisualStateManager.VisualStateGroups>
            <VisualStateGroup>
                <VisualState x:Name="DefaultLayout"/>
                <VisualState x:Name="Below768Layout"/>
            </VisualStateGroup>
        </VisualStateManager.VisualStateGroups>

    </Grid>
</Page>



